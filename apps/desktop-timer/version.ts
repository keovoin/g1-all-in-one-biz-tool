/**
 * @description
 * Current version of the Sastra Desktop Timer Application.
 *
 * @example
 * ```
 * import { version } from './version';
 *
 * console.log('Sastra Desktop Timer Application Version:', version);
 * ```
 *
 * @since 0.1.0
 */
export const version: string = require('./package.json').version;
