/**
 * @description
 * Current version of the Sastra Solution Server.
 *
 * @example
 * ```
 * import { version } from './version';
 *
 * console.log('Sastra Solution Server Version:', version);
 * ```
 *
 * @since 0.1.0
 */
export const version: string = require('./package.json').version;
