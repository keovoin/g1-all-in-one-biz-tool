import { Route } from '@angular/router';
/**
 * Creates the child routes for the Time Tracking module.
 *
 * @returns Angular Route array for the time tracking feature.
 */
export declare function createTimeTrackingRoutes(): Route[];
