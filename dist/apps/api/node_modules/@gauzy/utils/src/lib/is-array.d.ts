/**
 * Check if the item is an array.
 * @param item - The item to check.
 * @returns {boolean} - True if the item is an array, otherwise false.
 */
export declare function isArray(item: any): boolean;
