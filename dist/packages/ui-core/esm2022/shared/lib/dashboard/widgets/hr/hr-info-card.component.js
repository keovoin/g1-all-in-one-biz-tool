import { ChangeDetectionStrategy, Component, input, output } from '@angular/core';
import { NbButtonModule, NbIconModule } from '@nebular/theme';
import { TranslateModule } from '@ngx-translate/core';
import { InfoBlockModule } from '../../info-block/info-block.module';
import * as i0 from "@angular/core";
import * as i1 from "@nebular/theme";
import * as i2 from "../../info-block/info-block.component";
import * as i3 from "@ngx-translate/core";
/**
 * Presentational shell shared by the nine Human Resources info-block widgets.
 *
 * It wraps — and never re-implements — the existing `ga-info-block`, adding the
 * three states a canvas-hosted widget needs but the legacy Human Resources page
 * never had:
 *
 * - a **loading skeleton**, so the card never flashes a hard `0` that reads as
 *   real data while the request is still in flight;
 * - a recoverable **error state** with a retry button;
 * - an actionable **empty state**, because every figure on this dashboard is
 *   about ONE employee and there is nothing meaningful to show — least of all
 *   zeros — until one is selected.
 *
 * It deliberately renders NO card chrome and NO card header: on a canvas every
 * widget is already wrapped by `<ga-dashboard-widget-host>`, which owns the
 * `nb-card`, the header title and the edit-mode menu. The title passed as
 * {@link titleKey} is the *block's* own label — the left column of
 * `ga-info-block`'s title/value row and the accordion header — not a second card
 * header, which is why it stays the short legacy wording ("Total Income") rather
 * than repeating the host's palette title ("Employee Total Income").
 *
 * Purely presentational on purpose — all fetching lives in
 * `BaseHrInfoWidgetComponent`.
 */
export class HrInfoCardComponent {
    constructor() {
        /** Translation key of the block title. */
        this.titleKey = input('', ...(ngDevMode ? [{ debugName: "titleKey" }] : []));
        /** Translation key of the muted explanation under the title. */
        this.metaKey = input(null, ...(ngDevMode ? [{ debugName: "metaKey" }] : []));
        /** Interpolation parameters for {@link metaKey}. */
        this.metaParams = input(null, ...(ngDevMode ? [{ debugName: "metaParams" }] : []));
        /** Already formatted amount shown as the block's figure. */
        this.value = input('', ...(ngDevMode ? [{ debugName: "value" }] : []));
        /**
         * CSS colour applied to the amount.
         *
         * A custom property (`var(--color-success-default)`) rather than a hex, so the
         * figure stays legible in every theme — see {@link HR_BLOCK_COLORS}.
         */
        this.color = input('', ...(ngDevMode ? [{ debugName: "color" }] : []));
        /** Renders the emphasized variant the legacy page uses for Profit. */
        this.highlight = input(false, ...(ngDevMode ? [{ debugName: "highlight" }] : []));
        /** Rows shown in the accordion body; a non-empty list turns on the accordion. */
        this.rows = input([], ...(ngDevMode ? [{ debugName: "rows" }] : []));
        /** Shows the skeleton instead of the block. */
        this.loading = input(false, ...(ngDevMode ? [{ debugName: "loading" }] : []));
        /** Non-null switches the card into its error state. */
        this.error = input(null, ...(ngDevMode ? [{ debugName: "error" }] : []));
        /** False renders the "select an employee" empty state. */
        this.hasEmployee = input(false, ...(ngDevMode ? [{ debugName: "hasEmployee" }] : []));
        /**
         * Translation key of an explanation for why this figure cannot be shown for
         * the current organization (e.g. a bonus block whose bonus rule is not the
         * one the organization uses). `null` renders the figure normally.
         */
        this.unavailableKey = input(null, ...(ngDevMode ? [{ debugName: "unavailableKey" }] : []));
        /** The user clicked the block itself. */
        this.openInfo = output();
        /** The user clicked one of the accordion rows. */
        this.openRow = output();
        /** The user asked for a re-fetch from the error state. */
        this.retry = output();
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "21.0.7", ngImport: i0, type: HrInfoCardComponent, deps: [], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "17.0.0", version: "21.0.7", type: HrInfoCardComponent, isStandalone: true, selector: "ga-hr-info-card", inputs: { titleKey: { classPropertyName: "titleKey", publicName: "titleKey", isSignal: true, isRequired: false, transformFunction: null }, metaKey: { classPropertyName: "metaKey", publicName: "metaKey", isSignal: true, isRequired: false, transformFunction: null }, metaParams: { classPropertyName: "metaParams", publicName: "metaParams", isSignal: true, isRequired: false, transformFunction: null }, value: { classPropertyName: "value", publicName: "value", isSignal: true, isRequired: false, transformFunction: null }, color: { classPropertyName: "color", publicName: "color", isSignal: true, isRequired: false, transformFunction: null }, highlight: { classPropertyName: "highlight", publicName: "highlight", isSignal: true, isRequired: false, transformFunction: null }, rows: { classPropertyName: "rows", publicName: "rows", isSignal: true, isRequired: false, transformFunction: null }, loading: { classPropertyName: "loading", publicName: "loading", isSignal: true, isRequired: false, transformFunction: null }, error: { classPropertyName: "error", publicName: "error", isSignal: true, isRequired: false, transformFunction: null }, hasEmployee: { classPropertyName: "hasEmployee", publicName: "hasEmployee", isSignal: true, isRequired: false, transformFunction: null }, unavailableKey: { classPropertyName: "unavailableKey", publicName: "unavailableKey", isSignal: true, isRequired: false, transformFunction: null } }, outputs: { openInfo: "openInfo", openRow: "openRow", retry: "retry" }, ngImport: i0, template: "<!--\n\tState order is load-bearing. `loading` is checked BEFORE `hasEmployee` because\n\ta widget starts out loading with no context resolved yet \u2014 testing the\n\temployee first would flash \"No employee selected\" on every single paint.\n-->\n@if (error(); as message) {\n\t<div class=\"hr-info-state is-error\" role=\"alert\">\n\t\t<nb-icon icon=\"alert-triangle-outline\" status=\"danger\"></nb-icon>\n\t\t<p class=\"state-title\">{{ 'DASHBOARD_PAGE.BUILDER.WIDGETS.ERROR' | translate }}</p>\n\t\t<p class=\"state-hint\" [title]=\"message\">{{ message }}</p>\n\t\t<button nbButton ghost size=\"tiny\" status=\"basic\" type=\"button\" (click)=\"retry.emit()\">\n\t\t\t{{ 'DASHBOARD_PAGE.BUILDER.WIDGETS.RETRY' | translate }}\n\t\t</button>\n\t</div>\n} @else if (loading()) {\n\t<!-- <output> carries an implicit `status` live region, so no explicit role. -->\n\t<output\n\t\tclass=\"hr-info-skeleton\"\n\t\taria-busy=\"true\"\n\t\t[attr.aria-label]=\"'DASHBOARD_PAGE.BUILDER.WIDGETS.LOADING' | translate\"\n\t>\n\t\t<!-- `span`, not `div`: `<output>` only accepts phrasing content. -->\n\t\t<span class=\"skeleton-line skeleton-title\"></span>\n\t\t<span class=\"skeleton-line skeleton-meta\"></span>\n\t\t<span class=\"skeleton-line skeleton-value\"></span>\n\t</output>\n} @else if (!hasEmployee()) {\n\t<div class=\"hr-info-state is-empty\">\n\t\t<nb-icon icon=\"person-outline\"></nb-icon>\n\t\t<p class=\"state-title\">{{ 'DASHBOARD_PAGE.BUILDER.WIDGETS.HR.NO_EMPLOYEE' | translate }}</p>\n\t\t<p class=\"state-hint\">{{ 'DASHBOARD_PAGE.BUILDER.WIDGETS.HR.NO_EMPLOYEE_HINT' | translate }}</p>\n\t</div>\n} @else if (unavailableKey()) {\n\t<div class=\"hr-info-state is-empty\">\n\t\t<nb-icon icon=\"info-outline\"></nb-icon>\n\t\t<p class=\"state-title\">{{ 'DASHBOARD_PAGE.BUILDER.WIDGETS.HR.UNAVAILABLE' | translate }}</p>\n\t\t<p class=\"state-hint\">{{ unavailableKey() | translate }}</p>\n\t</div>\n} @else {\n\t<ga-info-block\n\t\t[accordion]=\"rows().length > 0\"\n\t\t[blockType]=\"highlight() ? 'highlight' : ''\"\n\t\t[title]=\"titleKey() | translate\"\n\t\t[meta]=\"metaKey() ? (metaKey() | translate: metaParams() ?? {}) : ''\"\n\t\t[value]=\"value()\"\n\t\t[color]=\"color()\"\n\t\t(openInfo)=\"openInfo.emit()\"\n\t>\n\t\t<!--\n\t\t\tProjected into `ga-info-block`'s accordion body. Only rendered when the\n\t\t\tblock is in accordion mode, which is exactly when `rows()` is non-empty.\n\t\t-->\n\t\t@for (row of rows(); track row.id) {\n\t\t\t<ga-info-block\n\t\t\t\tlistItem=\"true\"\n\t\t\t\t[title]=\"row.titleKey | translate\"\n\t\t\t\t[meta]=\"row.metaKey ? (row.metaKey | translate: row.metaParams ?? {}) : ''\"\n\t\t\t\t[value]=\"row.value\"\n\t\t\t\t[color]=\"row.color\"\n\t\t\t\t(openInfo)=\"openRow.emit(row)\"\n\t\t\t></ga-info-block>\n\t\t}\n\t</ga-info-block>\n}\n", styles: [":host{display:flex;flex-direction:column;justify-content:center;height:100%;width:100%;min-width:0;overflow:auto}:host ::ng-deep .info-block-wrapper:not(.info-block-wrapper--list-item){margin-bottom:0}.hr-info-state{display:flex;flex-direction:column;align-items:flex-start;gap:.25rem}.hr-info-state nb-icon{height:18px;width:18px;color:var(--text-hint-color)}.hr-info-state .state-title{margin:0;color:var(--text-basic-color);font-size:var(--text-subtitle-2-font-size);font-weight:var(--text-subtitle-2-font-weight)}.hr-info-state .state-hint{margin:0;color:var(--text-hint-color);font-size:var(--text-caption-font-size);overflow:hidden;text-overflow:ellipsis}.hr-info-state.is-error nb-icon{color:var(--color-danger-default)}.hr-info-state button{margin-top:.25rem}.hr-info-skeleton{display:flex;flex-direction:column;gap:.5rem;width:100%}.hr-info-skeleton .skeleton-line{border-radius:var(--border-radius);background:linear-gradient(90deg,var(--background-basic-color-2) 25%,var(--background-basic-color-3) 37%,var(--background-basic-color-2) 63%);background-size:400% 100%;animation:hr-info-skeleton-shimmer 1.4s ease infinite}.hr-info-skeleton .skeleton-title{height:14px;width:45%}.hr-info-skeleton .skeleton-meta{height:10px;width:70%}.hr-info-skeleton .skeleton-value{height:22px;width:35%}@media(prefers-reduced-motion:reduce){.hr-info-skeleton .skeleton-line{animation:none}}@keyframes hr-info-skeleton-shimmer{0%{background-position:100% 50%}to{background-position:0 50%}}\n/**\n * @license\n * Copyright Akveo. All Rights Reserved.\n * Licensed under the MIT License. See License.txt in the project root for license information.\n */\n"], dependencies: [{ kind: "ngmodule", type: NbButtonModule }, { kind: "component", type: i1.NbButtonComponent, selector: "button[nbButton],a[nbButton],input[type=\"button\"][nbButton],input[type=\"submit\"][nbButton]", inputs: ["hero"] }, { kind: "ngmodule", type: NbIconModule }, { kind: "component", type: i1.NbIconComponent, selector: "nb-icon", inputs: ["icon", "pack", "options", "status", "config"] }, { kind: "ngmodule", type: TranslateModule }, { kind: "ngmodule", type: InfoBlockModule }, { kind: "component", type: i2.InfoBlockComponent, selector: "ga-info-block", inputs: ["title", "meta", "value", "color", "blockType", "accordion", "listItem"], outputs: ["openInfo"] }, { kind: "pipe", type: i3.TranslatePipe, name: "translate" }], changeDetection: i0.ChangeDetectionStrategy.OnPush }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "21.0.7", ngImport: i0, type: HrInfoCardComponent, decorators: [{
            type: Component,
            args: [{ selector: 'ga-hr-info-card', standalone: true, imports: [NbButtonModule, NbIconModule, TranslateModule, InfoBlockModule], changeDetection: ChangeDetectionStrategy.OnPush, template: "<!--\n\tState order is load-bearing. `loading` is checked BEFORE `hasEmployee` because\n\ta widget starts out loading with no context resolved yet \u2014 testing the\n\temployee first would flash \"No employee selected\" on every single paint.\n-->\n@if (error(); as message) {\n\t<div class=\"hr-info-state is-error\" role=\"alert\">\n\t\t<nb-icon icon=\"alert-triangle-outline\" status=\"danger\"></nb-icon>\n\t\t<p class=\"state-title\">{{ 'DASHBOARD_PAGE.BUILDER.WIDGETS.ERROR' | translate }}</p>\n\t\t<p class=\"state-hint\" [title]=\"message\">{{ message }}</p>\n\t\t<button nbButton ghost size=\"tiny\" status=\"basic\" type=\"button\" (click)=\"retry.emit()\">\n\t\t\t{{ 'DASHBOARD_PAGE.BUILDER.WIDGETS.RETRY' | translate }}\n\t\t</button>\n\t</div>\n} @else if (loading()) {\n\t<!-- <output> carries an implicit `status` live region, so no explicit role. -->\n\t<output\n\t\tclass=\"hr-info-skeleton\"\n\t\taria-busy=\"true\"\n\t\t[attr.aria-label]=\"'DASHBOARD_PAGE.BUILDER.WIDGETS.LOADING' | translate\"\n\t>\n\t\t<!-- `span`, not `div`: `<output>` only accepts phrasing content. -->\n\t\t<span class=\"skeleton-line skeleton-title\"></span>\n\t\t<span class=\"skeleton-line skeleton-meta\"></span>\n\t\t<span class=\"skeleton-line skeleton-value\"></span>\n\t</output>\n} @else if (!hasEmployee()) {\n\t<div class=\"hr-info-state is-empty\">\n\t\t<nb-icon icon=\"person-outline\"></nb-icon>\n\t\t<p class=\"state-title\">{{ 'DASHBOARD_PAGE.BUILDER.WIDGETS.HR.NO_EMPLOYEE' | translate }}</p>\n\t\t<p class=\"state-hint\">{{ 'DASHBOARD_PAGE.BUILDER.WIDGETS.HR.NO_EMPLOYEE_HINT' | translate }}</p>\n\t</div>\n} @else if (unavailableKey()) {\n\t<div class=\"hr-info-state is-empty\">\n\t\t<nb-icon icon=\"info-outline\"></nb-icon>\n\t\t<p class=\"state-title\">{{ 'DASHBOARD_PAGE.BUILDER.WIDGETS.HR.UNAVAILABLE' | translate }}</p>\n\t\t<p class=\"state-hint\">{{ unavailableKey() | translate }}</p>\n\t</div>\n} @else {\n\t<ga-info-block\n\t\t[accordion]=\"rows().length > 0\"\n\t\t[blockType]=\"highlight() ? 'highlight' : ''\"\n\t\t[title]=\"titleKey() | translate\"\n\t\t[meta]=\"metaKey() ? (metaKey() | translate: metaParams() ?? {}) : ''\"\n\t\t[value]=\"value()\"\n\t\t[color]=\"color()\"\n\t\t(openInfo)=\"openInfo.emit()\"\n\t>\n\t\t<!--\n\t\t\tProjected into `ga-info-block`'s accordion body. Only rendered when the\n\t\t\tblock is in accordion mode, which is exactly when `rows()` is non-empty.\n\t\t-->\n\t\t@for (row of rows(); track row.id) {\n\t\t\t<ga-info-block\n\t\t\t\tlistItem=\"true\"\n\t\t\t\t[title]=\"row.titleKey | translate\"\n\t\t\t\t[meta]=\"row.metaKey ? (row.metaKey | translate: row.metaParams ?? {}) : ''\"\n\t\t\t\t[value]=\"row.value\"\n\t\t\t\t[color]=\"row.color\"\n\t\t\t\t(openInfo)=\"openRow.emit(row)\"\n\t\t\t></ga-info-block>\n\t\t}\n\t</ga-info-block>\n}\n", styles: [":host{display:flex;flex-direction:column;justify-content:center;height:100%;width:100%;min-width:0;overflow:auto}:host ::ng-deep .info-block-wrapper:not(.info-block-wrapper--list-item){margin-bottom:0}.hr-info-state{display:flex;flex-direction:column;align-items:flex-start;gap:.25rem}.hr-info-state nb-icon{height:18px;width:18px;color:var(--text-hint-color)}.hr-info-state .state-title{margin:0;color:var(--text-basic-color);font-size:var(--text-subtitle-2-font-size);font-weight:var(--text-subtitle-2-font-weight)}.hr-info-state .state-hint{margin:0;color:var(--text-hint-color);font-size:var(--text-caption-font-size);overflow:hidden;text-overflow:ellipsis}.hr-info-state.is-error nb-icon{color:var(--color-danger-default)}.hr-info-state button{margin-top:.25rem}.hr-info-skeleton{display:flex;flex-direction:column;gap:.5rem;width:100%}.hr-info-skeleton .skeleton-line{border-radius:var(--border-radius);background:linear-gradient(90deg,var(--background-basic-color-2) 25%,var(--background-basic-color-3) 37%,var(--background-basic-color-2) 63%);background-size:400% 100%;animation:hr-info-skeleton-shimmer 1.4s ease infinite}.hr-info-skeleton .skeleton-title{height:14px;width:45%}.hr-info-skeleton .skeleton-meta{height:10px;width:70%}.hr-info-skeleton .skeleton-value{height:22px;width:35%}@media(prefers-reduced-motion:reduce){.hr-info-skeleton .skeleton-line{animation:none}}@keyframes hr-info-skeleton-shimmer{0%{background-position:100% 50%}to{background-position:0 50%}}\n/**\n * @license\n * Copyright Akveo. All Rights Reserved.\n * Licensed under the MIT License. See License.txt in the project root for license information.\n */\n"] }]
        }], propDecorators: { titleKey: [{ type: i0.Input, args: [{ isSignal: true, alias: "titleKey", required: false }] }], metaKey: [{ type: i0.Input, args: [{ isSignal: true, alias: "metaKey", required: false }] }], metaParams: [{ type: i0.Input, args: [{ isSignal: true, alias: "metaParams", required: false }] }], value: [{ type: i0.Input, args: [{ isSignal: true, alias: "value", required: false }] }], color: [{ type: i0.Input, args: [{ isSignal: true, alias: "color", required: false }] }], highlight: [{ type: i0.Input, args: [{ isSignal: true, alias: "highlight", required: false }] }], rows: [{ type: i0.Input, args: [{ isSignal: true, alias: "rows", required: false }] }], loading: [{ type: i0.Input, args: [{ isSignal: true, alias: "loading", required: false }] }], error: [{ type: i0.Input, args: [{ isSignal: true, alias: "error", required: false }] }], hasEmployee: [{ type: i0.Input, args: [{ isSignal: true, alias: "hasEmployee", required: false }] }], unavailableKey: [{ type: i0.Input, args: [{ isSignal: true, alias: "unavailableKey", required: false }] }], openInfo: [{ type: i0.Output, args: ["openInfo"] }], openRow: [{ type: i0.Output, args: ["openRow"] }], retry: [{ type: i0.Output, args: ["retry"] }] } });
//# sourceMappingURL=hr-info-card.component.js.map