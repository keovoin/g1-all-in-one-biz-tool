import { type ReactNode, type CSSProperties } from 'react';
export interface CardActionProps {
    children?: ReactNode;
    style?: CSSProperties;
    className?: string;
}
/**
 * CardAction — interactive elements positioned in the header's top-right corner.
 *
 * Placed inside `<CardHeader>`, rendered absolutely in the top-right.
 */
export declare function CardAction({ children, style, className }: CardActionProps): import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=CardAction.d.ts.map